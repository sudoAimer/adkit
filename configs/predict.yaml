model:
  name: anomalydino
  weights: weights/dinov2_vits14/model.safetensors
  device: cuda
data:
  format: folder
  test_path: datasets/mvtec-ad/bottle/test/broken_large/000.png
  image_size: 448
run:
  mode: predict
  checkpoint: outputs/bottle_official/seed_0/model.pt
output:
  directory: outputs/reloaded_prediction
  save_maps: true
  visualize: true
evaluation:
  pro_fpr_limit: 0.3
